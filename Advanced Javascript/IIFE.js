//IIFE is a javascript function which is called as soon as it is defined.

// We need this in kind of a page load function call or priavte funcs.

// Using Grouping Operator making a private func:

(function () {
  console.log(`Hello I'm an IFFE function.`);
})();

// We can't access anything inside IIFE function from outside.
